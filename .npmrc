# CampaignBridge builds on the same reviewed Node and pnpm line as the other
# Aggressive WordPress products. Optional native lifecycle scripts are not
# required by this plugin's webpack build.
engine-strict=true
ignore-scripts=true
confirmModulesPurge=false
save-exact=false
strict-peer-dependencies=false
