/**
 * CampaignBridge Settings Page JavaScript
 *
 * Handles settings page interactions following WordPress standards.
 */

document.addEventListener("DOMContentLoaded", function () {
  // Any settings page specific JavaScript can be added here
  // Currently using URL-based tab navigation, so no custom tab switching needed
});
